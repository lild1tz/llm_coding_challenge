# Apollo: AI-Ассистент для Агрономов (LLM Coding Challenge - АгроПрогресс)

![Apollo Banner](https://yandex.ru/images/search?from=tabbar&img_url=https%3A%2F%2Fcdn.quotesgram.com%2Fimg%2F71%2F50%2F97584996-XHA1_039.jpg&lr=45&pos=1&rpt=simage&text=Apollo%20picture) 

**Apollo** — это интеллектуальный сервис, разработанный в рамках **LLM Coding Challenge** для компании **АгроПрогресс**. Его основная задача — преобразовывать неструктурированные текстовые и фотоотчеты агрономов о полевых работах в структурированные данные в формате таблицы. Это позволяет автоматизировать сбор данных, улучшить аналитику и повысить эффективность управления сельскохозяйственными операциями.

## 🎯 Проблема

Агрономы часто передают информацию о проделанной работе в виде свободных текстовых сообщений или фотографий таблиц. Ручная обработка таких данных занимает много времени, подвержена ошибкам и затрудняет оперативный анализ. Apollo решает эту проблему, автоматизируя процесс извлечения и структурирования ключевой информации.

## ✨ Возможности

*   **Обработка текстовых сообщений:** Apollo анализирует текстовые отчеты агрономов, извлекает данные (дата, подразделение, операция, культура, объем работ за день, накопленный объем, валовый сбор) и представляет их в виде таблицы JSON.
*   **Классификация сообщений:** Определяет, содержит ли входящее сообщение информацию о полевых операциях, с указанием вероятности. Это помогает отфильтровывать релевантные отчеты.
*   **Обработка фотоотчетов:** Сервис может принимать фотографии (например, рукописных таблиц или экранов), распознавать и структурировать информацию с них.
*   **Нормализация данных:** Использует предопределенные справочники культур (`cultures`), операций (`operations`) и подразделений (`units`) для стандартизации извлеченных данных.
*   **Гибкость:** Способен обрабатывать сообщения различной структуры и полноты.
*   **Масштабируемость:** Упакован в Docker-контейнер для легкого развертывания и масштабирования.

## 🛠️ Технологический стек

*   **Бэкенд:** Python, FastAPI
*   **LLM Интеграция:** Langchain, OpenAI (GPT модели)
*   **Машинное обучение (Классификация):** Transformers (для эмбеддингов), ONNX Runtime (для инференса модели эмбеддингов), Scikit-learn (Логистическая регрессия)
*   **Контейнеризация:** Docker, Docker Compose
*   **Управление зависимостями:** pip, `requirements.txt`
*   **Конфигурация:** Python-dotenv

## 📁 Структура проекта

### Запуск контейнера
```docker
docker-compose up --build
```

## 🚀 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <your-repository-url>
    cd <repository-directory>
    ```

2.  **Создайте файл `.env`:**
    Создайте файл `.env` в корне проекта и добавьте в него необходимые переменные окружения. Как минимум, вам понадобится ключ API для OpenAI:
    ```dotenv
    OPENAI_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    OPENAI_MODEL=gpt-4o # Или другая мультимодельная модель, например GigaChat Max, Gimini и т.д.
    # OPENAI_BASE_URL= # Если используете прокси или альтернативный эндпоинт OpenAI
    # LANGSMITH_TRACING="true" # Для трассировки LangSmith (опционально)
    # LANGSMITH_ENDPOINT="https://api.smith.langchain.com"
    # LANGSMITH_API_KEY=ls__xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    # LANGSMITH_PROJECT="Your-Project-Name"
    BERT_MODEL="sentence-transformers/all-MiniLM-L6-v2" # Модель для эмбеддингов, если отличается от используемой
    ```

3.  **Установите Docker и Docker Compose:**
    Убедитесь, что на вашей машине установлены Docker и Docker Compose. [Инструкция по установке Docker](https://docs.docker.com/engine/install/), [Инструкция по установке Docker Compose](https://docs.docker.com/compose/install/).

4.  **Соберите и запустите сервис с помощью Docker Compose:**
    ```bash
    docker-compose up --build -d
    ```
    Флаг `--build` пересобирает образ, если были изменения. Флаг `-d` запускает контейнер в фоновом режиме.

5.  **Проверка:**
    Сервис будет доступен по адресу `http://localhost:8000`. Вы можете проверить его работоспособность, открыв `http://localhost:8000/docs` в браузере (Swagger UI для интерактивного тестирования API).

## ⚙️ API Эндпоинты

Сервис предоставляет следующие эндпоинты:

### 1. `/process_message` (POST)

Принимает текстовое сообщение и возвращает структурированную таблицу.

**Запрос:**
*   **Тело запроса:** JSON объект с ключом `message`.
    ```json
    {
      "message": "Пахота зяби под сою\nПо ПУ 7/1402\nОтд 17 7/141\n\nВырав-ие зяби под кук/силос\nПо ПУ 16/16\nОтд 12 16/16"
    }
    ```

**Ответ (успешный, 200 OK):**
*   **Тело ответа:** JSON объект с ключом `table`, содержащим список структурированных записей.
    ```json
    {
      "table": [
        {
          "date": null,
          "division": "АОР",
          "operation": "Пахота",
          "culture": "Соя",
          "per_day": 7,
          "per_operation": 1402,
          "val_day": null,
          "val_beginning": null
        },
        {
          "date": null,
          "division": "АОР",
          "operation": "Выравнивание зяби",
          "culture": "Кукуруза кормовая",
          "per_day": 16,
          "per_operation": 16,
          "val_day": null,
          "val_beginning": null
        }
      ]
    }
    ```

### 2. `/classify_message` (POST)

Принимает текстовое сообщение и классифицирует его (является ли оно отчетом об операции).

**Запрос:**
*   **Тело запроса:** JSON объект с ключом `message`.
    ```json
    {
      "message": "СЗР оз пш - 103/557 га"
    }
    ```

**Ответ (успешный, 200 OK):**
*   **Тело ответа:** JSON объект с вероятностью принадлежности к классу "операция" и предсказанным классом (1 - операция, 0 - не операция).
    ```json
    {
      "probability": 0.98, // Примерное значение
      "prediction": 1
    }
    ```

### 3. `/process_photo` (POST)

Принимает изображение (в формате base64) и его тип, возвращает структурированную таблицу.

**Запрос:**
*   **Тело запроса:** JSON объект с ключами `photo` (строка base64) и `type` (тип изображения: "png", "jpeg" или "jpg").
    ```json
    {
      "photo": "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII=", // Пример очень короткой строки base64
      "type": "png"
    }
    ```

**Ответ (успешный, 200 OK):**
*   **Тело ответа:** Аналогично ответу `/process_message`.
    ```json
    {
      "table": [
        // ... структурированные данные с фото ...
      ]
    }
    ```

## 💡 Примеры использования (`curl`)

```bash
# Обработка текстового сообщения
curl -X POST "http://localhost:8000/process_message" \
-H "Content-Type: application/json" \
-d '{
  "message": "10.03 день\n2-я подкормка озимых, ПУ \"Юг\" - 1749/2559\nОтд11 - 307/307"
}'

# Классификация сообщения
curl -X POST "http://localhost:8000/classify_message" \
-H "Content-Type: application/json" \
-d '{
  "message": "Какая завтра погода?"
}'

# Обработка фото (замените BASE64_STRING на реальную строку base64)
curl -X POST "http://localhost:8000/process_photo" \
-H "Content-Type: application/json" \
-d '{
  "photo": "BASE64_STRING",
  "type": "jpeg"
}'
```

## 🔮 Возможные улучшения

*   **Улучшение распознавания фото:** Интеграция с более продвинутыми OCR-решениями или fine-tuning модели для специфических форматов таблиц.
*   **Обработка голосовых сообщений:** Добавление возможности транскрибации и последующей обработки голосовых отчетов.
*   **Расширение справочников:** Добавление большего количества культур, операций, единиц измерения, типов техники и т.д.
*   **Валидация данных:** Более строгая валидация извлеченных числовых данных (например, проверка на реалистичность объемов).
*   **Обратная связь:** Механизм для пользователей (агрономов или администраторов) для коррекции ошибок распознавания, что может использоваться для дообучения моделей.
*   **Асинхронная обработка:** Для очень больших сообщений или большого количества запросов можно реализовать асинхронную обработку с использованием очередей задач (Celery, RabbitMQ).
*   **Интеграция с другими системами:** Разработка API для интеграции с существующими системами учета и аналитики АгроПрогресс.

---

Сделано с ❤️ для LLM Coding Challenge (АгроПрогресс).
